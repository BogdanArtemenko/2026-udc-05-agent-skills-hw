{
  "name": "udc-ws05-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Tiny widget-registry library — target for the WS5 Agent Skills exercises.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "node scripts/build.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "esbuild": "^0.24.0"
  }
}
